# Handoff — 2026-04-28 (Phase 2C in progress)

## სად ვართ

Phase 2 super-admin ფიჩები (audit log, login-as, regenerate-key) მზადაა და production-ზე LIVE. **შემდეგი ფაზა: 2C** — owner-ის გადაწყვეტილებების მიხედვით:

1. **„login-as" ღილაკი წავშალო** — owner-ი ფიქრობდა რომ კლიენტის ანგარიშში სილენტურად შესვლა არასწორია. customer-ი თვითონ უნდა იყოს ანგარიშის მართვა-ში.
2. **Trial = 10 დღე → ავტო-suspend** — გადახდის გარეშე 10 დღის შემდეგ ავტომატურად ჩაიკეტოს. owner-ი „გადახდა მიიღე"-ს დააჭერს როცა ფული მოვა → ცოცხალი იქნება სანამ owner არ გაყინავს.
3. **Customer self-service** (commit B, შემდეგი) — `/admin/settings` გვერდი: change password + logout all devices.

## რა მქონდა ჩაკომიტული მაგრამ ვერ დავტესტე

Commit `2f9d056` — **WIP**, code მზადაა, smoke test interrupted-ი.

**ცვლილებები:**
- `server.py`: `/api/super/tenants/{id}/login-as` + `/admin/stop-impersonation` ამოშლა
- `admin-super.html`: „შესვლა" ღილაკი ამოშლა + loginAs() function ამოშლა; trial countdown გამოჩენა
- `src/db.py`: `tenants.trial_ends_at` column + backfill + `TRIAL_DURATION_DAYS = 10` + `create_tenant`-ში 10-day trial set + `update_tenant_status('active')`-ში clear
- `src/auth.py`: middleware-ში `_trial_expired()` + auto-suspend logic (ორივე APIKeyMiddleware-ში და AdminSessionMiddleware-ში)

## რა დარჩა Commit A-სთვის (აქამდე ყოფილი)

1. **Smoke test** ლოკალურად:
   - ახალი tenant შეიქმნას → `trial_ends_at` = now + 10 days
   - past `trial_ends_at` დაყენება + login → /api/inventory unable to access (403)
   - „გადახდა მიიღე" → `trial_ends_at` clears, status='active'
   - login_as endpoint → 404/405

2. **Push** — `git push origin feature/photo-analysis`

3. **Production-ზე verify** — Railway migration backfills existing trials.

## რა მინდა Commit B-სთვის (Customer self-service)

`admin_users` ცხრილს დამატება:
- `session_epoch INTEGER NOT NULL DEFAULT 0` — bump → all old sessions invalid

Token format change (in `src/sessions.py`):
- `issue_session_token` უნდა დაამატოს `epoch` field-ი payload-ში
- `load_session_token` უნდა შემოიყვანოს epoch
- middleware უნდა შეადაროს token's epoch admin_user.session_epoch-ს, mismatch → 401

ახალი endpoints:
- `POST /api/admin/change-password` — old + new, verify old, hash new, bump epoch
- `POST /api/admin/logout-all-devices` — bump epoch only (no password change)

ახალი HTML გვერდი:
- `/admin/settings` — sidebar-ში „⚙️ პარამეტრები" link
- ფორმა: ძველი პაროლი, ახალი, განმეორებითი → submit
- ღილაკი: „🚪 სხვა მოწყობილობებიდან გასვლა"

## არ უნდა დავივიწყო

- **Phase 2 ფიჩები** Production-ზე უკვე live-ია (audit log, regenerate-key)
- **Phase 1 ფიჩები** ასევე live (feature_set/pricing_tier, pricing matrix, FK constraints)
- Owner: `owner@tissu.shop` / `TissuUxUh7gP0K330bg!A1`
- Production URL: `https://tissu-agent-production.up.railway.app`
- Default super-admin password: ცვალებადი
- Storefront key: `tissu_ro_R_MScRpIDWmkCVHEmkuhTX_kU9Kbzdsg`
- Admin key: `tissu_admin_-fdyuZNlsk5F7Jsk0TYA9b9rkWrrvJFn`

## ღია კითხვა owner-ისთვის (Phase 1-ის ნაშთი)

Frontend Claude-მ კითხა:
- Pricing — owner ცარიელად ტოვებს `/admin/super/pricing`-ში, მერე ხელით შეავსებს
- Pricing strategy: hybrid (setup fee + monthly) ან pure SaaS — owner-ი ფიქრობდა

## შემდეგი ნაბიჯი

`/compact`-ის შემდეგ:
1. წაიკითხე ეს ფაილი
2. გაუშვი smoke test ლოკალურად
3. Push to Railway
4. შემდეგ Commit B (settings page)
