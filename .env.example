ANTHROPIC_API_KEY=sk-ant-your-key-here
LLM_PROVIDER=anthropic
LLM_MODEL=claude-sonnet-4-20250514
DATABASE_URL=sqlite:///data/agents.db
API_HOST=0.0.0.0
API_PORT=8000

# Required. Any request to /api/* (except /api/health, the Meta
# data-deletion callback, and the owner-confirm/photo-confirm links that
# the shop owner opens from WhatsApp) must send this key in the
# X-API-Key header. Generate a long random string for production.
ADMIN_API_KEY=replace-me
